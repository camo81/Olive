<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Olive.View.Settings" Title="Settings">
  <ScrollView>
    <StackLayout>
   
      <Entry Text="{Binding Username}" Placeholder="{Binding UsernamePlaceholder}"/>
      <Entry IsPassword="true" Text="{Binding Password , Mode=TwoWay}" Placeholder="{Binding PasswordPlaceholder}"/>
      <Entry Text="{Binding IpAddress , Mode=TwoWay}" Placeholder="{Binding IpAddressPlaceholder}"/>
      <Entry Text="{Binding IpAddressExt , Mode=TwoWay}" Placeholder="{Binding IpAddressExtPlaceholder}" />

      <Button x:Name = "buttonIns" Text="{Binding SaveSettingsText}" Command="{Binding SaveSettings}" Style="{StaticResource buttonStyle}"/>
      <Button x:Name = "buttonDel" Text="{Binding DelSettingsText}" Command="{Binding DelSettings}" Style="{StaticResource buttonStyle}" />      

      <Label TextColor="#77d065" FontSize = "20" Text="{ Binding OpStatus }" />

      <!-- <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />-->
      
    </StackLayout>


  </ScrollView>

</ContentPage>